import { StyleProps } from "../../types";

export const Style: StyleProps = {
  Box: {
    height: "90vh",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    flexDirection: "column",
  },
  text: {
    textAlign: "center"
  }
};